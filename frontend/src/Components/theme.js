import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    customColor: {
      main: '#613DC1', // TODO: Home Page color, figure out how to add more later
    },
  },
});

export default theme;
